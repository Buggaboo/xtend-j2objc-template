include ':shared'

// Travis CI doesn't support Android on OS X
if (!System.getenv('ANDROID_BUILD_DISABLED').equals('true')) {
    include ':app'
}

include(':Xtendroid')
project(':Xtendroid').projectDir = new File(settingsDir, '../../Xtendroid/Xtendroid')
